services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - "8050:8050"
    environment:
      POSTGRES_HOST: middleware-db-1
      POSTGRES_PORT: 5432
      POSTGRES_USER: simon
      POSTGRES_PASSWORD: simon123
      POSTGRES_DB: iotplant
    volumes:
      - .:/app
    networks:
      - middleware-network

networks:
  middleware-network:
    external: true